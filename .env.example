# =============================
# Project Environment Template
# =============================
# Copy this to .env (or .env.local for local dev) and fill values.
# Do NOT commit real secrets.

# -----------------------------
# MCP & Taskmaster (Cursor MCP)
# -----------------------------
# Model/API keys used by MCP servers and task-master-ai
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
PERPLEXITY_API_KEY=
GOOGLE_API_KEY=
XAI_API_KEY=
OPENROUTER_API_KEY=
MISTRAL_API_KEY=
AZURE_OPENAI_API_KEY=
# If you protect your local Ollama with a token (usually not needed)
OLLAMA_API_KEY=

# HTTP/OpenAPI/Postgres/Ollama MCP config
# Comma-separated OpenAPI spec URLs to load (e.g., https://api.myapp.com/openapi.json,https://other.com/spec.yaml)
OPENAPI_SPEC_URLS=
# Postgres connection string (MCP Postgres + backend tools). For Supabase use pooled connection string if available.
DATABASE_URL=
# Local Ollama base URL
OLLAMA_BASE_URL=http://localhost:11434
# Safety allowlist for Shell MCP (comma-separated commands)
ALLOWED_COMMANDS=npm,node,npx,supabase,ollama,curl

# -----------------------------
# Application (shared)
# -----------------------------
# Supabase project settings (Frontend uses NEXT_PUBLIC_ vars)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only key for privileged operations (NEVER expose on client)
SUPABASE_SERVICE_ROLE_KEY=
# Optional: direct DB connection for server tools/migrations
SUPABASE_DB_URL=

# LLM service for backend API routes
# If using local Ollama from server-side only
SERVER_OLLAMA_BASE_URL=http://localhost:11434

# Base app URLs
# Used for OAuth callbacks and absolute links
NEXT_PUBLIC_APP_URL=
APP_INTERNAL_API_BASE_URL=

# -----------------------------
# Frontend (Next.js)
# -----------------------------
# If you call internal APIs via absolute URLs in the browser
NEXT_PUBLIC_API_BASE_URL=
# Optional analytics/feature flags
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=
NEXT_PUBLIC_SENTRY_DSN=

# -----------------------------
# Backend (API routes / services)
# -----------------------------
# Observability/monitoring
SENTRY_DSN=
# Optional: Redis or queue backends if added later
REDIS_URL=
# Optional: rate limit or feature flags
RATE_LIMIT_POINTS=
RATE_LIMIT_DURATION_SEC=

# -----------------------------
# Misc / Local Dev
# -----------------------------
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1
